// TODO: Fire off AJAX request to backend to run filter.
//    Callback takes HTML results and replaces $('#product-results-list') content
$('#filter-form').on('change', function() { console.log($('#filter-form').serialize());});
